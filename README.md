# openvpn-shell-client


very simple openvpn client for Debian-based OS'es.

replace DNS'es and domain policy if necessary in src/client.sh

Install with 

```shell
chmod +x install.sh
./install sh
```
